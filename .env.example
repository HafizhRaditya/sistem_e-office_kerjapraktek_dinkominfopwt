APP_NAME="Sistem E-Office"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# UI language is Indonesian (users are ASN); see lang/id/validation.php.
# Fallback stays English so an untranslated key degrades instead of showing the raw key.
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# Reverse proxy (Nginx) in front of the app. '*' trusts any proxy, which is safe
# while the app is only reachable through it; set the proxy's IP if that changes.
TRUSTED_PROXIES=*

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# PostgreSQL is required by the domain migrations (CHECK constraints and
# expression indexes). PHPUnit overrides only DB_DATABASE to sistem_eoffice_test.
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=sistem_eoffice
DB_USERNAME=postgres
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_PATH=/
SESSION_DOMAIN=null

# Set both to true in production (HTTPS only).
# SESSION_SECURE_COOKIE=true sends the session cookie over HTTPS only; left unset
# it also travels over plain HTTP, where it can be captured on the network.
# SESSION_ENCRYPT=true encrypts session payloads at rest in the sessions table.
# Keep both false locally, where the dev server runs on plain HTTP.
SESSION_SECURE_COOKIE=false
SESSION_ENCRYPT=false

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Cloudflare Turnstile (FR-A02). Leave empty to skip verification in local dev.
TURNSTILE_SITEKEY=
TURNSTILE_SECRET=

VITE_APP_NAME="${APP_NAME}"
